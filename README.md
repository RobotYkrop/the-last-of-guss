The Last of Guss – Быстрое руководство
Браузерный кликер‑гонка за гусака‑мутанта G‑42

📖 О проекте
The Last of Guss — однопользовательская игра, где участники стараются собрать как можно больше тапов по виртуальному гусу. Игра реализована на современном стеке React + TypeScript и построена по Feature‑Sliced Design (FSD).

🛠 Технологический стек
Категория	Технология
Frontend	React 18, TypeScript
Routing	React‑Router v6
State	Zustand
UI	Tailwind CSS
Build	Vite
Архитектура	Feature‑Sliced Design (FSD)
🌐 Backend API
Параметр	Значение
Base URL	http://v2991160.hosted-by-vdsina.ru
Swagger UI	<BASE_URL>/docs
Scalar reference	<BASE_URL>/reference
Auth‑header	Authorization: Bearer <access_token>
Эндпоинты
Метод	Путь	Описание	Пример ответа
POST	/login	Авторизация пользователя	{ "access_token": "...", "token_type": "Bearer", "is_admin": false }
GET	/rounds	Список раундов	Round[]
GET	/rounds/:id	Детали одного раунда	Round
POST	/rounds	Создание нового раунда	Round
POST	/rounds/:id/tap	Тап по гусю	{ "score": number, "taps": number }
Типы (TypeScript)
export interface User {
  username: string;
  isAdmin: boolean;
  token: string;
}

export interface Round {
  id: string;
  start_time: string;   // ISO‑строка
  end_time: string;     // ISO‑строка
  status: 'active' | 'cooldown' | 'finished' | 'pending';
  totalScore?: number;
}

export interface Score {
  username: string;
  score: number;
}
📂 Структура проекта (FSD)
src
├─ app                 # Провайдеры, роутер, глобальные стили
│   ├─ providers
│   └─ styles
│   └─ App.tsx
│   └─ main.tsx
├─ pages               # Слои «страницы»
│   ├─ login/ui/LoginPage.tsx
│   ├─ rounds-list/ui/RoundsListPage.tsx
│   └─ round-detail/ui/RoundDetailPage.tsx
├─ widgets             # UI‑блоки (самостоятельные, без бизнес‑логики)
│   ├─ header/ui/Header.tsx
│   ├─ round-card/ui/RoundCard.tsx
│   └─ goose-clicker/ui/GooseClicker.tsx
├─ features            # Фичи – бизнес‑логика + UI
│   ├─ auth
│   │   ├─ login/ui/LoginForm.tsx
│   │   └─ logout/ui/LogoutButton.tsx
│   ├─ round
│   │   ├─ create-round/ui/CreateRoundButton.tsx
│   │   └─ tap-goose/model/useTap.ts
│   └─ timer
│       ├─ ui/CountdownTimer.tsx
│       └─ model/useCountdown.ts
├─ entities            # Сущности предметной области
│   ├─ user
│   │   ├─ model/types.ts
│   │   ├─ model/store.ts
│   │   └─ api/userApi.ts
│   └─ round
│       ├─ model/types.ts
│       ├─ model/store.ts
│       ├─ api/roundApi.ts
│       └─ ui/{RoundInfo.tsx, RoundStatus.tsx}
└─ shared              # Общее без бизнес‑логики
    ├─ api/base.ts
    ├─ config/constants.ts
    ├─ lib/hooks/useLocalStorage.ts
    ├─ lib/utils/{formatDate.ts, formatTime.ts}
    └─ ui/{Button/Button.tsx, Input/Input.tsx, Card/Card.tsx}
Rule of thumb: слой может импортировать только нижележащие слои, а публичный API экспортируется через index.ts.

🚀 Быстрый старт
1️⃣ Пререквизиты
Требование	Как установить
Node ≥ 18	nvm install 18 && nvm use 18
npm (или pnpm, yarn)	поставляется вместе с Node
Git	git --version
Docker (опционально)	docker --version
2️⃣ Клонирование репозитория
git clone https://github.com/your-org/the-last-of-guss.git
cd the-last-of-guss
3️⃣ Установка зависимостей
npm ci          # чистая установка без изменения package-lock.json
4️⃣ Переменные окружения
# создаём .env из шаблона
cp .env.example .env
Откройте .env и при необходимости поменяйте URL API:

VITE_API_BASE_URL=http://v2991160.hosted-by-vdsina.ru
# VITE_API_BASE_URL=http://localhost:3000   # локальный бекенд (если есть)
NB: Vite подхватывает только переменные, начинающиеся с VITE_.

5️⃣ Запуск в режиме разработки
npm run dev      # http://localhost:5173 (по умолчанию)
HMR (hot‑module‑replacement) обновит UI без полной перезагрузки страницы.

6️⃣ Сборка для production
npm run build    # → ./dist
npm run preview  # локальный preview готового билда
Содержимое папки dist/ можно разместить на любом статическом хостинге (Netlify, Vercel, GitHub Pages и т.д.).

7️⃣ Docker (опционально)
Dockerfile уже включён в репозиторий.

# собрать образ
docker build -t the-last-of-guss .

# запустить контейнер (порт 80 → 80)
docker run -d -p 80:80 --name guss the-last-of-guss
Контейнер использует nginx для отдачи готового билда.

🧪 Тестирование
Команда	Что делает
npm run test	Unit‑тесты (Jest + Testing‑Library)
npm run test:e2e	E2E‑тесты (Playwright)
npm run lint	ESLint + Prettier (авто‑исправление npm run lint:fix)
При сбоях в тестах проверьте, что Node и npm версии соответствуют требованиям, а node_modules актуальны (npm ci).

🔐 Авторизация (первый вход)
Откройте http://localhost:5173/login.
Введите username / password уже существующего пользователя.
Если аккаунт не создан, его можно добавить через API:

curl -X POST "$VITE_API_BASE_URL/users" \
  -H "Content-Type: application/json" \
  -d '{"username":"demo","password":"demo123"}'
После удачной аутентификации токен сохраняется в localStorage и автоматически добавляется к каждому запросу (Authorization: Bearer <token>).

🏗 Принципы FSD (коротко)
Слой	Назначение	Что может импортировать
app	Инициализация, глобальные провайдеры, роутинг	Никакой (только внешние библиотеки)
pages	Структурные «экранные» компоненты	app, widgets, features, entities, shared
widgets	Самостоятельные UI‑блоки	Всё ниже, кроме pages
features	Бизнес‑логика + UI для конкретных сценариев	entities, shared
entities	Модели предметной области, API, хранилища	shared
shared	UI‑kit, утилиты, типы, константы	Никакой (самый нижний уровень)
Каждый слой может импортировать только нижележащие слои и всегда использует публичный API (index.ts).

🤝 Как внести свой вклад
Fork репозиторий.

Создайте ветку feature/awesome‑feature или bugfix/description.

Внесите изменения, убедитесь, что проходят линт/тесты:

npm run lint && npm run test
Отправьте PR в main. В описании укажите:

Что делается (фиксы, новые фичи).
Как тестировать (команды, ручные шаги).
При добавлении новых модулей придерживайтесь FSD‑принципов: создайте соответствующую директорию (features/…, widgets/…), разместите ui и model‑сегменты, экспортируйте через index.ts.

📄 Лицензия
Проект распространяется под лицензией MIT. Полный текст лицензии находится в файле LICENSE в корне репозитория.

📞 Поддержка
Issues – любые баги, вопросы или предложения публикуйте в разделе Issues репозитория.
Telegram‑чат (необязательно) – https://t.me/the-last-of-guss.